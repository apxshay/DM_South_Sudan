## Project Overview

This repository contains a university project for a Master's Degree course in Computer Engineering and Artificial Intelligence, focused on Data Management.

The main objective is to compare a traditional Relational Database Management System (RDBMS) with a Graph Database using the same real-world dataset.

The selected case study concerns humanitarian and infrastructure data from **South Sudan**, including:

* Road networks
* Hospitals
* Displacement sites / refugee camps

**GitHub repository:** [github.com/apxshay/DM_South_Sudan](https://github.com/apxshay/DM_South_Sudan)

The project will involve:

1. Data acquisition and exploration
2. Data cleaning and preprocessing
3. Data modeling
4. Relational database implementation
5. Graph database implementation
6. Query design
7. Transaction management
8. Benchmarking and performance evaluation
9. Comparative analysis and reporting

The final goal is not only to build two databases, but also to understand and demonstrate the strengths and weaknesses of relational and graph-based approaches when working with interconnected infrastructure data.

---

## Your Role

You are the **Orchestrator Agent**.

You operate at the highest level of the project hierarchy.

Unlike specialized agents, your responsibility is not to perform a single task. Instead, you must maintain a global understanding of the project and coordinate the overall workflow.

You should:

* Understand the project architecture.
* Keep track of the current project status.
* Review outputs generated by specialized agents.
* Detect inconsistencies between project components.
* Suggest next steps.
* Identify missing pieces.
* Maintain coherence between datasets, schemas, ETL pipelines, benchmarks, and reports.

You are allowed to inspect every folder and every project artifact.

Think of yourself as a Technical Lead supervising the work of multiple specialized contributors.

---

## Project Methodology

The project will be developed incrementally.

Before implementing anything, data must be understood.

The expected workflow is:

### Phase 1 — Data Understanding

Analyze all datasets and produce:

* Dataset descriptions
* Schemas
* Data types
* Missing values analysis
* Uniqueness and identifier analysis
* Cross-dataset observations
* Data quality issues

### Phase 2 — Data Modeling

Prepare import-ready datasets and schemas:

* Reconcile health facility sources (WHO 2025 and SS 2023) into one canonical table
* Connect health facilities and displacement sites to the road graph (nearest intersection node + connector edge)
* Design relational schema (PostgreSQL/PostGIS) and graph schema (Neo4j)
* Document decisions and outputs in `docs/phase2_data_modeling.md`

The two models should represent the same domain while remaining natural for their respective database technologies.

### Phase 3 — Data Integration

Build ETL pipelines that:

* Load raw datasets
* Clean data
* Normalize formats
* Generate import-ready outputs

### Phase 4 — Database Population

Populate:

* PostgreSQL (or equivalent RDBMS)
* Neo4j (or equivalent Graph Database)

### Phase 5 — Query Design

Create equivalent queries for both systems.

Examples:

* Nearest hospital to a displacement site
* Reachability analysis
* Route discovery
* Infrastructure impact analysis
* Shortest path computation

### Phase 6 — Benchmarking

Compare:

* Insert performance
* Update performance
* Query execution time
* Complexity of implementation
* Expressiveness of query language

### Phase 7 — Final Evaluation

Produce a rigorous comparison between the two database paradigms.

---

## Coordination Rules

When working on any task:

1. First understand the current state of the repository.
2. Avoid making assumptions that contradict existing work.
3. Prefer incremental improvements over large rewrites.
4. Document important decisions.
5. Keep outputs reproducible.
6. Flag uncertainties explicitly.
7. When a specialized agent should handle a task, clearly recommend which type of agent should be responsible.

---

## Current Status

**Phase 1 — Data Understanding: COMPLETE**

All raw datasets have been downloaded, profiled, and documented. Visual validation maps are available for both raw data and the processed road graph.

**Road network topology: COMPLETE** (2026-06-24)

An OSMnx-based driveable road graph for South Sudan has been built with proper T-junction connectivity. Processed node/edge layers and an interactive topology validation map are available locally.

**Phase 2 — Data Modeling: IN PROGRESS**

Health facility reconciliation complete (2,251 canonical facilities; 2,017 with valid coordinates; state codes harmonized to SS00–SS10). Network integration and database schemas pending.

Regenerate Phase 2 outputs: `python scripts/merge_health_facilities.py` or `./scripts/bootstrap.sh --from merge`.

### Repository layout

```
data_management/
├── AGENT.md                          # This file
├── README.md                         # Setup and usage instructions
├── environment.yml                   # Conda/Miniforge environment (recommended on Windows)
├── requirements.txt
├── data/
│   ├── raw/                          # HDX datasets + Geofabrik OSM extract (excluded from git)
│   │   ├── roads/                    # Humanitarian basemap (976 segments)
│   │   ├── roads_hotosm/             # HOT OSM roads (182,544 raw → 20,512 filtered)
│   │   ├── health_facilities/        # WHO 2025 + SS 2023 master lists
│   │   ├── idp/                      # IDMC annual + disaster CSVs
│   │   └── displacement_sites/       # IOM DTM site assessments (rounds 4–11)
│   ├── processed/                    # Generated graph layers (excluded from git)
│   │   ├── roads_hotosm/             # OSMnx road nodes, edges, topology_summary.json
│   │   ├── health_facilities/        # Phase 2: canonical merged facilities (planned)
│   │   └── network/                  # Phase 2: POI nodes, connector edges (planned)
│   └── interim/                      # OSMnx build cache (excluded from git)
│       └── osmnx/
├── docs/
│   ├── phase1_data_understanding.md  # Full Phase 1 analysis report
│   ├── phase1_profile.json           # Machine-readable profiles
│   ├── road_network_topology.md      # Road graph extraction report
│   └── phase2_data_modeling.md       # Phase 2 progress log
├── output/                           # Generated HTML maps (excluded from git)
├── scripts/
│   ├── setup.ps1                     # Windows + Miniforge bootstrap
│   ├── setup.sh                      # macOS/Linux venv bootstrap
│   ├── download_datasets.py          # Cross-platform HDX downloader
│   ├── download_datasets.sh          # Unix wrapper for download_datasets.py
│   ├── create_dirs.py                # Create data/output directories
│   ├── explore_datasets.py           # Regenerate phase1_profile.json
│   ├── visualize_data_validation.py  # Phase 1 Folium validation map
│   ├── build_road_network_topology.py # OSMnx road graph extraction
│   ├── visualize_road_topology.py    # Topology validation map (toggleable nodes/edges)
│   ├── merge_health_facilities.py    # Phase 2: WHO + SS 2023 canonical merge
│   ├── health_facility_admin.py      # Phase 2: admin/state harmonization helpers
│   ├── bootstrap.ps1 / bootstrap.sh  # Full pipeline runner (all platforms)
│   ├── setup_conda.sh                # macOS/Linux conda bootstrap
│   ├── resolve_python.sh             # Resolve conda env or venv python
│   └── project_paths.py              # Shared paths and directory bootstrap
└── src/                              # Placeholder for future code
    ├── etl/
    ├── db/
    ├── queries/
    └── benchmarks/
```

### Datasets acquired

| Domain | Source | Local path | Records (key figure) |
|--------|--------|------------|----------------------|
| Roads (humanitarian) | [HDX — South Sudan Road Network](https://data.humdata.org/dataset/south-sudan-road-network_hdx) | `data/raw/roads/` | 976 line features (roads + rivers) |
| Roads (HOT OSM) | [HDX — Roads of South Sudan](https://data.humdata.org/dataset/hotosm_ssd_roads) | `data/raw/roads_hotosm/` | 20,512 filtered lines (`primary`, `secondary`, `tertiary`, `unclassified`) |
| Health facilities | [HDX — Health Facilities](https://data.humdata.org/dataset/south-sudan-health-facilities) | `data/raw/health_facilities/` | WHO 2025: 1,988 rows; SS 2023: 1,513 rows (75 hospitals) |
| IDP displacements | [HDX — IDMC IDPs](https://data.humdata.org/dataset/idmc-idp-data-ssd) | `data/raw/idp/` | 15 annual + 158 disaster events (no coordinates) |
| Displacement sites | [HDX — IOM DTM Site Assessment](https://data.humdata.org/dataset/south-sudan-displacement-data-site-assessment-iom-dtm) | `data/raw/displacement_sites/` | 77 sites in Round 11 (100% GPS coverage) |

Raw data is **not committed to git** (see `.gitignore`). Re-download with `./scripts/download_datasets.sh`.

### Resolved decisions (carry into Phase 2)

| Topic | Decision |
|-------|----------|
| Primary road network | OSMnx processed graph (`data/processed/roads_hotosm/`) |
| Highway filter | `primary`, `secondary`, `tertiary`, `unclassified` |
| Displacement sites for spatial graph | IOM DTM **Round 11** (77 sites, full GPS) |
| IDMC national IDP data | Context only — not part of the spatial graph |
| POI–road linking (Phase 2) | Snap each facility/camp to nearest road intersection node; add connector edge |

### Road network topology summary (2026-06-24)

1. **OSMnx pipeline:** Geofabrik South Sudan OSM extract → highway filter → `graph_from_xml(simplify=True)`.
2. **Processed graph:** 24,779 nodes, 62,345 edges, 17,795 junction nodes, ~79,442 km total length.
3. **T-junction connectivity:** handled by OSMnx; raw HDX line shapefiles are not used for routing.

Full report: `docs/road_network_topology.md`

### Phase 1 key findings

1. **Two independent road networks** exist: a small humanitarian basemap (976 features, includes rivers) and a much larger HOT OSM export (20,512 filtered road segments). They share no segment identifier.
2. **Health facilities** come in two overlapping files with different schemas. SS 2023 has facility type and codes; WHO 2025 has more rows and better coordinate coverage.
3. **IDMC IDP data** provides national/disaster aggregates only — no georeferenced camp locations.
4. **IOM DTM site assessments** provide georeferenced displacement sites with admin P-codes and per-site IDP counts. This fills the camp-location gap identified during early exploration.
5. **HOT OSM filter applied:** only `highway` ∈ {`primary`, `secondary`, `tertiary`, `unclassified`} retained (saved to `data/raw/roads_hotosm/filtered/roads_lines_filtered.gpkg`).

Full analysis: `docs/phase1_data_understanding.md`

### Artifacts produced

| Artifact | Path | In git? |
|----------|------|---------|
| Phase 1 report | `docs/phase1_data_understanding.md` | Yes |
| Machine-readable profile | `docs/phase1_profile.json` | Yes |
| Road topology report | `docs/road_network_topology.md` | Yes |
| Phase 2 progress log | `docs/phase2_data_modeling.md` | Yes |
| Phase 1 validation map | `output/south_sudan_data_validation.html` | No (regenerate locally) |
| Topology validation map | `output/south_sudan_road_topology_validation.html` | No (regenerate locally) |
| Road graph nodes | `data/processed/roads_hotosm/road_nodes.gpkg` | No (regenerate locally) |
| Road graph edges | `data/processed/roads_hotosm/road_edges.gpkg` | No (regenerate locally) |
| Topology summary | `data/processed/roads_hotosm/topology_summary.json` | No (regenerate locally) |

**Phase 1 validation map** shows:
- HOT OSM roads (4 toggleable highway-class layers)
- IOM DTM displacement sites (red markers, Round 11)
- Hospitals from SS 2023 master list (green markers, 68 with valid coordinates)

**Topology validation map** shows:
- Road network nodes (24,779 — toggleable layer)
- Road edges by highway class (62,345 total — toggleable layers)
- Displacement sites and hospitals (same sources as Phase 1 map)

Full topology methodology: `docs/road_network_topology.md`

### Bootstrap on a new machine

**Windows 10 + Miniforge (recommended):**

```powershell
.\scripts\setup.ps1
conda activate dm-south-sudan
.\scripts\bootstrap.ps1
```

**macOS / Linux + Miniforge (recommended):**

```bash
chmod +x scripts/*.sh
./scripts/setup_conda.sh
conda activate dm-south-sudan
./scripts/bootstrap.sh
```

**macOS / Linux (venv fallback):**

```bash
./scripts/setup.sh
./scripts/bootstrap.sh
```

**Resume after clone/pull** (raw/processed data are gitignored):

```bash
./scripts/bootstrap.sh --skip-download          # reuse data/raw/
./scripts/bootstrap.sh --from merge             # Phase 2 merge only
```

**Conda on any OS (manual):**

```bash
conda env create -f environment.yml
conda activate dm-south-sudan
python scripts/create_dirs.py
python scripts/download_datasets.py
python scripts/build_road_network_topology.py
python scripts/merge_health_facilities.py
```

GeoPandas/GDAL should be installed via **conda-forge** (`environment.yml`), not pip alone. Road topology requires **osmium-tool** (conda-forge) and **osmnx** (pip).

---

## Next Expected Milestone

**Phase 2 — Data Modeling**

1. **Health facility reconciliation** — merge WHO 2025 (1,988 rows) and SS 2023 (1,513 rows) into one canonical dataset; resolve duplicate codes, schema differences, and missing coordinates (Phase 1 Section 4).
2. **Network integration** — for each health facility (~1,900 with coordinates) and displacement site (77, IOM DTM Round 11), find the nearest road intersection node and add a connector edge to the augmented graph.
3. **Schema design** — document relational (PostgreSQL/PostGIS) and graph (Neo4j) models aligned with the augmented network.
4. **Reporting** — log progress in `docs/phase2_data_modeling.md` after each major step.

**Resolved:** primary road network (OSMnx graph), displacement round (Round 11), POI linking method (snap to nearest road node + connector edge).

**Still open:** exact health-facility merge rules; handling facilities without coordinates; IDMC data role in schemas (national context only).

Reference documents:
- `docs/phase1_data_understanding.md`
- `docs/road_network_topology.md`

---

## Success Criteria

A successful project should demonstrate:

* Correct data modeling in both paradigms
* Meaningful real-world queries
* Reproducible experiments
* Fair benchmarking methodology
* Clear comparison between relational and graph databases
* Well-documented engineering decisions

Always prioritize scientific rigor, reproducibility, and clarity.
