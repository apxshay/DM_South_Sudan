# Data exploration and processing
# NOTE: On Windows, use environment.yml (conda-forge) for geopandas/GDAL.
#       pip-only installs of geopandas may fail without GDAL system libraries.
pandas>=2.0
numpy>=1.24
openpyxl>=3.1

# Geospatial (roads network)
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
folium>=0.15
osmnx>=2.0

# Database drivers
psycopg2-binary>=2.9
neo4j>=5.0

# ETL utilities
python-dotenv>=1.0
requests>=2.31

# Notebooks
jupyter>=1.0
matplotlib>=3.7

# Benchmarking
tabulate>=0.9
